import { Observable } from 'rxjs';
import { PreviewValue, SchemaType } from '@sanity/types';
import type { Path, PrepareViewOptions } from './types';
import { Previewable } from './types';
export interface PreparedSnapshot {
    type?: SchemaType;
    snapshot: undefined | PreviewValue;
}
export declare function isRecord(value: unknown): value is Record<string, unknown>;
export declare function isReference(value: unknown): value is {
    _ref: string;
};
export declare function createPreviewObserver(observePaths: (value: Previewable, paths: Path[]) => any): (value: Previewable, type: SchemaType, viewOptions?: PrepareViewOptions) => Observable<PreparedSnapshot>;
//# sourceMappingURL=createPreviewObserver.d.ts.map